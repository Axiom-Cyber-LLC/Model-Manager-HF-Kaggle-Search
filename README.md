# Model Manager HF/Kaggle Search

This public repository has been temporarily sanitized while the publishable
source package is rebuilt.

The prior public branch contents included local development paths that should
not have been published. The next public commit should be rebuilt from a clean
export, reviewed for private paths and generated artifacts, and then pushed
only after validation.

## Current Status

- Public branch is intentionally minimal.
- Do not add local model caches, scan results, chat exports, binary model files,
  scanner checkouts, virtual environments, or machine-specific paths.
- Rebuild the public package from a reviewed export rather than copying a local
  working directory wholesale.
